# C++ 競技プログラミング学習計画書 (LEARNING_PLAN.md)

本ドキュメントは、競技プログラミング（AtCoder等）に向けたC++（C++20基準）の基礎再定着から典型データ構造・頻出アルゴリズムの実装力、デバッグ技術の確立までを体系的に進めるための学習計画書です。

---

## メンター基本規約（指導方針）

1. **自力実装の絶対原則**: メンター（AI）は、ユーザーから明確な指示があるまで**完成コードを直接提示しません**。
2. **思考支援**: 「要件定義」「ヒント」「計算量の示唆」「着眼点」を提供し、ユーザー自身が設計・実装を行います。
3. **多角的レビュー**: 提出されたコードに対し、以下の観点から厳格なレビューとフィードバックを行います。
   - **計算量**: 時間計算量・空間計算量（オーダー記法 $O(\cdot)$）
   - **未定義動作 (UB)**: 配列外参照、初期化漏れ、境界値オーバーフロー、イテレータ無効化
   - **エッジケース**: $N=0, 1$、空データ、極大・極小値、負数
   - **言語作法・メモリ効率**: 参照渡し、コピーコスト、モダンC++機能（C++20）の適切な活用

---

## 技術スタック & 設計方針

- **言語仕様**: C++20
  - `<bit>` ヘッダ（`std::popcount` 等）、構造化束縛、ローカル再帰ラムダ（`auto dfs = [&](auto self, ...) -> void`）など
- **コンパイル & デバッグ環境**:
  - GCC / Clang
  - サニタイザ標準適用: `-fsanitize=address,undefined -D_GLIBCXX_DEBUG`
- **テスト・ツールチェーン**:
  - `online-judge-tools` (`oj`) によるサンプルケース自動テスト
  - VS Code GUI デバッガ（GDB / LLDB）によるブレークポイント・ステップ実行
- **重要視する着眼点**:
  - $O(N)$ と $O(\log N)$ の計算量の差
  - 値渡しによる意図しないコピーコスト TLE
  - `std::vector` 等の動的変更に伴うイテレータ無効化
  - `size_t`（符号なし型）の減算によるアンダーフロー（ラップアラウンド）

---

## メンター運用プロトコル（学習サイクル）

```mermaid
graph LR
    A[Step 1: 学習項目宣言] --> B[Step 2: ミニ課題/問い出題]
    B --> C[Step 3: ユーザー自力実装]
    C --> D[Step 4: メンターレビュー]
    D -->|修正が必要| C
    D -->|合格!| E[Step 5: チェック & 次の項目へ]
```

- **[Step 1] 宣言**: ユーザーが「Phase X の 〇〇 に取り組みます」と宣言する。
- **[Step 2] 出題**: メンターが該当テーマに関する「理解度確認の問い」または「実装すべきミニ課題（仕様・制約・入出力例）」を出題する（※解答コードは出さない）。
- **[Step 3] 実装**: ユーザーが考察・設計し、コードまたは解答を提示する。
- **[Step 4] レビュー**: メンターが計算量、コーナーケース、未定義動作、C++作法の観点でレビュー。
- **[Step 5] 完了**: 合格基準を満たしたら、計画書の該当項目にチェック（`- [x]`）を付けて次へ進む。

---

## カリキュラム・チェックリスト

### Phase 1: 基礎構文・STL・メモリモデルの再定着
C++特有のメモリモデル、計算量トラップ、型安全性を把握し、堅牢な基本コードを書けるようにする。

- [x] **1.1 入出力高速化とテンプレート設計**
  - `std::cin.tie(nullptr)`, `std::ios::sync_with_stdio(false)` の原理
  - `'\n'` と `std::endl` のフラッシュコスト差
  - 基本テンプレートの構築（型エイリアス、マクロに頼りすぎないモダン記法）
- [x] **1.2 整数型とオーバーフロー対策**
  - `int` (32bit: $\approx 2 \times 10^9$) と `long long` (64bit: $\approx 9 \times 10^{18}$) の境界
  - リテラルの型と罠（`1 << k` vs `1LL << k`、暗黙の型昇格）
  - 乗算時のオーバーフロー検知とキャストタイミング
- [x] **1.3 ペア・タプル・構造化束縛**
  - `std::pair`, `std::tuple` の活用と `{}` による直接初期化
  - C++17/20 構造化束縛（`auto [u, v, cost] = edge;`）の記法と参照束縛（`auto& [x, y]`）
- [x] **1.4 イテレータの仕組みと区間概念**
  - 半開区間 `[begin, end)` の設計思想
  - ランダムアクセスイテレータ（`std::vector`, `std::deque`） vs 双方向イテレータ（`std::set`, `std::list`）
  - イテレータの演算（`+ k` が使える型・使えない型）と `std::advance`, `std::distance`
- [x] **1.5 参照渡し・コピーコスト・符号なし型の罠**
  - 値渡し vs `const auto&` 参照渡しのコスト差（vector や string を引数にする際の TLE 回避）
  - `container.size()` が `size_t`（符号なし）であることによる減算トラップ（`v.size() - 1` のアンダーフロー）
  - 範囲for文（`for (const auto& x : v)` / `for (auto& x : v)`）の使い分け

---

### Phase 2: 典型データ構造と標準アルゴリズム
STLコンテナの内部構造（平衡二分探索木、ヒープ等）を理解し、適切な計算量で使いこなす。

- [x] **2.1 `std::priority_queue` の実践**
  - デフォルトの最大ヒープ（降順）構造
  - `std::greater<T>` による最小ヒープ（昇順）化
  - カスタム比較関数/構造体の適用（ダイクストラ法等への応用）
- [ ] **2.2 平衡二分探索木コンテナ（`set`, `multiset`, `map`）**
  - 赤黒木による各操作の $O(\log N)$ 保証
  - メンバ関数 `s.lower_bound()` vs 汎用アルゴリズム `std::lower_bound()` の致命的計算量差（$O(\log N)$ vs $O(N)$）
  - `multiset` の要素削除における `erase(val)`（全削除）と `erase(iterator)`（1要素削除）の違い
- [ ] **2.3 座標圧縮の実装パイプライン**
  - 連続した値への写像の考え方
  - イディオム: `std::sort` $\to$ `std::unique` $\to$ `vec.erase` $\to$ `std::lower_bound`
  - 重複排除と元のインデックス復元のフロー
- [ ] **2.4 ビット演算とビット全探索**
  - 基本論理演算（`&`, `|`, `^`, `~`, `<<`, `>>`）
  - 部分集合の全探索（$2^N$ ループ）
  - C++20 `<bit>` ヘッダ（`std::popcount`, `std::has_single_bit`, `std::countl_zero` 等）の活用

---

### Phase 3: 頻出アルゴリズムの実装と未定義動作（UB）耐性
典型アルゴリズムを自力でゼロから構築し、バグを生まない設計パターンを体得する。

- [ ] **3.1 累積和・2次元累積和**
  - 1次元累積和による区間和 $O(1)$ クエリ
  - 2次元累積和の領域加算・領域和クエリ
  - 1-indexed 設計による境界値（添字 `-1`）ガードと実装の簡潔化
- [ ] **3.2 素集合データ構造 (Union-Find)**
  - 経路圧縮 (Path Compression) とサイズマージ (Union by size) によるならし計算量 $\alpha(N)$
  - 連結成分のサイズ管理、連結判定、リーダー取得
  - よくあるミス（マージ時の根の更新漏れ、親配列の初期化ミス）
- [ ] **3.3 グラフ探索（DFS / BFS / 0-1 BFS）**
  - 隣接リスト表現（`std::vector<std::vector<Edge>>`）
  - ローカル再帰ラムダ式を用いた DFS 実装（`auto dfs = [&](auto self, int u) -> void`）
  - `std::queue` を用いた最短路 BFS（重み1）
  - `std::deque` を用いた 0-1 BFS（重み0と1）の計算量 $O(V + E)$
- [ ] **3.4 剰余演算（Modular Arithmetic）**
  - 負の数の剰余補正（`((x % MOD) + MOD) % MOD`）
  - 繰り返し二乗法による $a^b \pmod M$ の $O(\log b)$ 計算
  - フェルマーの小定理による逆元計算（MOD が素数の場合）

---

### Phase 4: 実践演習・デバッグワークフロー
コンテスト本番で遭遇する RE / TLE / WA を即座に切り分けるための環境・デバッグ力を鍛える。

- [ ] **4.1 サニタイザ（ASan / UBSan / `_GLIBCXX_DEBUG`）の活用**
  - 配列外参照（Heap/Stack buffer overflow）の検出
  - 未初期化変数、符号付き整数オーバーフローの検出
  - `_GLIBCXX_DEBUG` による無効なイテレータ参照・範囲外アクセスの即時トラップ
- [ ] **4.2 VS Code GUI デバッガ環境の確立**
  - GDB / LLDB の `launch.json`, `tasks.json` 構成
  - ブレークポイント、ウォッチ式、コールスタックの確認
  - 再帰関数やループ内の状態遷移の追跡
- [ ] **4.3 `online-judge-tools` (`oj`) テストパイプライン**
  - 問題サンプルのダウンロード（`oj dl <URL>`）
  - コンパイル＆一括テスト（`oj test`）
  - 自作コーナーケースの追加検証フロー
